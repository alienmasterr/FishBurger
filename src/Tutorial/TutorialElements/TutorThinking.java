package Tutorial.TutorialElements;

import Elements.Node;

public class TutorThinking extends Node {
    public TutorThinking(int x, int y, int width, int height) {
        super(x, y, width, height);
        image = getImage("/tutorial/thinking.PNG");
    }
}
